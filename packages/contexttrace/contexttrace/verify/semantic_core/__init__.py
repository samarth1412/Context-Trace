"""Successor-verifier boundary for generic semantic verification logic.

The calibrated implementation remains frozen in :mod:`contexttrace.verify.facts`.
New generic logic belongs here only after the untouched-test manifest is published
and the successor verifier receives a distinct version identifier.
"""
