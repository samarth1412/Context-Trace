# Transformer Attention

Transformer models use self-attention to let each token attend to other tokens in the sequence. Multi-head attention allows the model to represent several relationship patterns in parallel.

The attention mechanism is not the same as a citation. Attention weights do not prove that a generated claim is supported by an external source.

