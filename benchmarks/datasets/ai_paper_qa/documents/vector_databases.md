# Vector Databases

Vector databases store embeddings and support nearest-neighbor search over high-dimensional representations. They are commonly used to retrieve semantically similar chunks for RAG applications.

Index choice, chunk size, embedding model, and metadata filters can change retrieval recall and latency. Production systems usually measure both quality and cost when tuning a vector store.

